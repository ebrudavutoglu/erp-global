FROM node:20-alpine

WORKDIR /app

# Copy monorepo
COPY . .

# Install dependencies
RUN npm ci

# Expose port
EXPOSE 3000

# Start dev server for web
CMD ["npm", "run", "dev", "--", "--filter=@erp/web"]
