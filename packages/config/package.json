{
  "name": "@repo/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared configuration files",
  "main": "index.js",
  "exports": {
    "./eslint": "./eslint-preset.js",
    "./tsconfig": "./tsconfig"
  },
  "scripts": {
    "build": "echo 'Config package has no build'",
    "type-check": "echo 'Config package has no type checking'"
  }
}
